interface:
  display_name: "Cross-Surface PR Review"
  short_description: "Review shared bug classes across protection surfaces"
  default_prompt: "Use $cross-surface-pr-review to classify this PR's mechanism across the auth, edge, installer, client, and packaging surfaces."
policy:
  allow_implicit_invocation: false
