# VISION

- 状态：第一阶段开发中
- 更新日期：2026-08-20

## 下一步方向

dsh-auth 当前已具备多账号身份基础与可信团队预览。预览阶段提供独立账号、独立浏览器会话、当前身份展示、管理员创建/禁用成员；所有账号仍共享同一个 Harness 实例权限。

下一阶段演进为真正的多账号协作：

1. **协作会话目录**：支持查看团队授权范围内的会话列表、成员状态和会话归属。
2. **评论与 prompt 协作**：在不冒充他人的前提下，为会话增加明确作者的评论/建议/队列提交。
3. **个人私有工作区**：每个账号拥有默认私有工作区，其他账号不可见。
4. **共享（团队）工作区**：管理员创建团队工作区并指定成员，成员共享访问。
5. **受监管访问**：如需管理员查看私有空间，应采用显式授权、强通知、限时访问和审计。

## 原则

- 保持现有架构不变量：Caddy 唯一公网边界、Harness 只绑 loopback、通过 Cordis 扩展缝集成，不修改 Harness。
- 保持现有会话与安全语义（滚动会话、服务端撤销、安全 Cookie）。
- 开工前先从已安装的 Harness 版本核实工作区与身份扩展点，不猜测 API。
- 在 Harness 暴露可验证的 Principal/Policy/Event/Tool 扩展点之前，不宣称私人空间、逐会话 ACL 或可审计 prompt 作者已经完成。

## 非目标

自助注册、SaaS 多租户、完整 RBAC、跨实例共享。
